// PostCSS configuration — Angular's build pipeline reads this automatically.
// tailwindcss: processes @tailwind directives and generates utility classes.
// autoprefixer: adds vendor prefixes (e.g. -webkit-) for browser compatibility.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
