/*
 * GalleryComponent — the "/gallery" route
 *
 * Placeholder page. Replace the template content when the gallery
 * feature is built. Navbar and Footer are provided by AppComponent.
 */
import { Component } from '@angular/core';

@Component({
  selector: 'app-gallery',
  standalone: true,
  imports: [],
  templateUrl: './gallery.component.html',
})
export class GalleryComponent {}
